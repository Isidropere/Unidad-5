@model IEnumerable<CatalogoProductoDesarrolloWebUASD.Models.ModelDB.FormaPago>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoVentasID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FormaPago1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoVentasID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FormaPago1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FormaPagoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FormaPagoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FormaPagoID })
        </td>
    </tr>
}

</table>
